</$objtype/mkfile

# T1 unit tests.  Each test is a self-contained program that exits
# non-zero and prints a one-line "FAIL: reason" when it fails.  To
# add one: write foo.c here and add foo to TESTS.
TESTS=\
	csumtest\
	structtest\
	geomtest\
	devtest\
	supertest\
	montest\
	fmtcktest\
	storetest\
	objtest\
	scrubtest\
	committest\
	enumtest\
	maptest\
	bmrebuildtest\
	peeropstest\
	srvtest\
	srviotest\

LIB=../lib/libshoal.a$O
SRVLIB=../srv/libshoalsrv.a$O
HFILES=../lib/shoal.h ../lib/store.h t1.h
LDFLAGS=

PROGS=${TESTS:%=$O.%}

default:V:	all

all:V:	$PROGS

$O.%:	%.$O $LIB
	$LD $LDFLAGS -o $target $prereq

# srvtest and srviotest drive the 9P surface, so they link the server
# library too and are libthread programs; every other test's link line
# is unchanged.
$O.srvtest:	srvtest.$O $SRVLIB $LIB
	$LD $LDFLAGS -o $target $prereq

srvtest.$O:	srvtest.c srv9p.h ../srv/srv.h $HFILES
	$CC $CFLAGS srvtest.c

$O.srviotest:	srviotest.$O $SRVLIB $LIB
	$LD $LDFLAGS -o $target $prereq

srviotest.$O:	srviotest.c srv9p.h ../srv/srv.h $HFILES
	$CC $CFLAGS srviotest.c

%.$O:	%.c $HFILES
	$CC $CFLAGS $stem.c

test:V:	$PROGS
	for(t in $PROGS){
		echo '	'^$t
		./$t || exit 'test failed'
	}

clean nuke:V:
	rm -f *.[$OS] [$OS].* $CLEANFILES
